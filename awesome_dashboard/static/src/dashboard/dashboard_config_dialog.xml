<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="awesome_dashboard.DashboardConfigurationDialog">
        <Dialog title="'Configuration'" footer="true">
            <div class="list-group">
                <t t-foreach="state.items" t-as="item" t-key="item.id">
                    <div class="list-group-item d-flex align-items-center">
                        <CheckBox value="item.isEnabled" label="item.description" onChange="(val) => item.isEnabled = val"/>
                    </div>
                </t>
            </div>
            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="onApply">Apply</button>
                <button class="btn btn-secondary" t-on-click="props.close">Cancel</button>
            </t>
        </Dialog>
    </t>
</templates>
