### Added
- Added demo data

### Refactored
- Refactored test code:
  - Used `self.env.ref()` for references to demo data instead of creating records in `setUp`.

### Removed
- Deprecated lines from `ir_cron.xml`.
